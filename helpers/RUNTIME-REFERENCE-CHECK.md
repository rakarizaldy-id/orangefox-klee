# Helper Runtime Reference Check

Search target: reconstructed Step9 `recovery/root`.

## `aw86927_ff_prime_auto`

References found:
- **None.** It is not referenced by the reconstructed final Build60 runtime scripts/init files.

This strongly suggests it is historical/bring-up residue rather than a final runtime dependency.

## `aw86927_ff_constant_prime_noplay`

Directly referenced by:
- `recovery/root/system/bin/klee_haptic_stock_init.sh`

Therefore this helper is a required final runtime dependency and should be reconstructed first.
