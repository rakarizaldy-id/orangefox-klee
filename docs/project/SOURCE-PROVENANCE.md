# Source Provenance / History Reconstruction

This FINAL MASTER release reconstructs the project from three evidence layers:

1. **Authoritative MASTER HANDOFF ZIP**
   `OFOX-klee-MASTER-HANDOFF-CHAT1-to-HYPERDOT-SPARSE-20260819`
   - contains the consolidated Chat1→Build59 timeline;
   - retained early Chat1→Build23 handoffs;
   - retained Build33 and Build34–44 histories;
   - retained Build45–59 build reports, patches and runtime evidence;
   - retained HyperDot sparse/SUPER investigation.

2. **Current Chat9 runtime session**
   - HyperDot OrangeFox ZIP final test;
   - post-reboot SUPER/mapping validation;
   - Format Data clean-install path;
   - discovery of Build59 post-format touch loss;
   - manual `start touch_report` root-cause proof;
   - Build60 patch;
   - repeated Build60 Format Data tests;
   - post-System Build60 regression;
   - Fenrir -> mandatory Format Data -> successful System boot.

3. **Preserved historical release reports**
   - Universal region-aware FBE history;
   - fastbootd Windows validation;
   - AW86927 haptic build reports;
   - Build47 Fenrir and Build58 wipe acceptance reports.

## Important documentation rule
Early project activity before tracked Build6 is intentionally summarized as **Foundation**.
The available handoffs do not support a trustworthy per-build reconstruction for Build1–5.
This package therefore does not invent those build numbers.

Rejected experiments are retained in the full history because they explain why the final architecture
looks the way it does, but they are clearly classified as rejected/diagnostic rather than shipped features.
