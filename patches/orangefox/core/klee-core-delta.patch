# Documentation-only representation. Use apply-klee-source-patches.py.
--- a/variables.h
+++ b/variables.h
@@
-#define FOX_BRANCH        <upstream value>
+#define FOX_BRANCH        "klee"

--- a/partition.cpp
+++ b/partition.cpp
@@ TWPartition::UnMount(bool Display_Error, int flags)
-		umount2(Mount_Point.c_str(), flags);
-		if (Is_Mounted()) {
+		errno = 0;
+		umount2(Mount_Point.c_str(), flags);
+		const int unmount_errno = errno;
+		if (Is_Mounted()) {
+			if (Mount_Point == "/odm" && unmount_errno == EBUSY) {
+				(void)umount2(Mount_Point.c_str(), MNT_DETACH);
+				usleep(200000);
+				if (!Is_Mounted())
+					return true;
+				errno = unmount_errno;
+			}
